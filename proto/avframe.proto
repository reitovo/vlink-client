syntax = "proto3";

message AvPacket {
	bytes data = 1;
	int64 pts = 2;
	int64 dts = 3;
}

message VtsAvFrame { 
  repeated AvPacket rgbPackets = 2;
  repeated AvPacket aPackets = 3;
  int64 pts = 4;
}
